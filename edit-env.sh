#!/bin/bash

# Скрипт для редактирования файла .env

echo "📝 Открываю файл .env для редактирования..."
echo ""
echo "После редактирования:"
echo "1. Сохраните файл (Ctrl+O, Enter, Ctrl+X в nano)"
echo "2. Сервер перезапустится автоматически"
echo ""
echo "Нажмите Enter для продолжения..."
read

# Проверяем, какой редактор доступен
if command -v nano &> /dev/null; then
    nano .env
elif command -v vi &> /dev/null; then
    vi .env
elif command -v vim &> /dev/null; then
    vim .env
else
    echo "❌ Не найден текстовый редактор. Откройте файл .env вручную:"
    echo "   $(pwd)/.env"
fi
