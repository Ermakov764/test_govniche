# 📦 Storage Application

Полнофункциональное приложение для управления файлами с поддержкой локального хранилища (без AWS) и AWS S3.

## 🚀 Возможности

- ✅ Загрузка файлов (одиночная и множественная)
- ✅ Просмотр списка всех файлов
- ✅ Скачивание файлов
- ✅ Превью файлов через подписанные URL
- ✅ Удаление файлов (одиночное и множественное)
- ✅ Drag & Drop загрузка
- ✅ Выбор нескольких файлов для массовых операций
- ✅ Красивый современный UI
- ✅ Адаптивный дизайн

## 📋 Требования

- Node.js 16+ и npm
- **Для локального режима:** ничего дополнительного не требуется! 🎉
- **Для AWS S3 режима:** AWS аккаунт с настроенным S3 bucket и credentials

## 🛠️ Установка

### 0. 🎯 Выбор режима работы

Приложение поддерживает **два режима**:

#### ✅ Режим 1: Локальное хранилище (РЕКОМЕНДУЕТСЯ для учебных проектов)

**Не требует регистрации в AWS!** Файлы сохраняются локально в папке `storage/`.

**Настройка:**
1. Откройте файл `.env`
2. Убедитесь, что `USE_LOCAL_STORAGE=true` (уже установлено по умолчанию)
3. Готово! Можно сразу использовать приложение

**Быстрое редактирование .env:**
```bash
./edit-env.sh
```

#### ☁️ Режим 2: AWS S3 (для продакшена)

Требует регистрации в AWS. Подробная инструкция:
👉 **[AWS_SETUP_GUIDE.md](AWS_SETUP_GUIDE.md)**

**Настройка:**
1. Создайте аккаунт на [aws.amazon.com](https://aws.amazon.com)
2. Получите AWS credentials (IAM → Create User → Create Access Key)
3. Создайте S3 bucket
4. В файле `.env` установите `USE_LOCAL_STORAGE=false`
5. Заполните AWS credentials в `.env`

### 1. Установите зависимости

```bash
# Установка всех зависимостей (бэкенд + фронтенд)
npm run install-all
```

Или по отдельности:

```bash
# Бэкенд зависимости
npm install

# Фронтенд зависимости
cd client && npm install
```

### 2. Настройте переменные окружения

Файл `.env` уже создан и настроен на **локальное хранилище** по умолчанию.

**Для локального режима (по умолчанию):**
```env
USE_LOCAL_STORAGE=true
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:3000
```

**Для AWS S3 режима:**
```env
USE_LOCAL_STORAGE=false
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_REGION=us-east-1
AWS_S3_BUCKET_NAME=your-bucket-name
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:3000
```

### 3. Настройте AWS S3

1. Создайте S3 bucket в AWS Console
2. Настройте CORS для bucket (если нужно):

```json
[
  {
    "AllowedHeaders": ["*"],
    "AllowedMethods": ["GET", "PUT", "POST", "DELETE", "HEAD"],
    "AllowedOrigins": ["http://localhost:3000"],
    "ExposeHeaders": []
  }
]
```

3. Убедитесь, что у вашего IAM пользователя есть права:
   - `s3:PutObject`
   - `s3:GetObject`
   - `s3:DeleteObject`
   - `s3:ListBucket`
   - `s3:CreateBucket` (если bucket еще не создан)

## 🎯 Запуск приложения

### Режим разработки (бэкенд + фронтенд одновременно)

```bash
npm run dev
```

Это запустит:
- Бэкенд сервер на `http://localhost:5000`
- Фронтенд на `http://localhost:3000`

### Запуск по отдельности

**Бэкенд:**
```bash
npm run server
```

**Фронтенд:**
```bash
npm run client
```

## 📚 Документация

- **[IDF (Interface Definition)](docs/IDF.md)** — описание API: эндпоинты, структуры данных, коды ошибок
- **[UML диаграммы](uml/README.md)** — архитектура и диаграммы проекта
- **[Быстрый старт](QUICK_START.md)** — запуск без AWS

## 🦀 C++ Backend

Доступна C++ версия бэкенда в папке `cpp/`:

```bash
cd cpp
mkdir build && cd build
cmake ..
make
./storage_server
```

Полностью совместима с React фронтендом. Требуется CMake 3.14+ и компилятор C++17.

## 📡 API Endpoints

### Загрузка файлов

**POST** `/api/s3/upload`
- Загрузка одного файла
- Body: `multipart/form-data` с полем `file`

**POST** `/api/s3/upload-multiple`
- Загрузка нескольких файлов
- Body: `multipart/form-data` с полем `files` (массив)

### Получение файлов

**GET** `/api/s3/files`
- Получить список всех файлов

**GET** `/api/s3/files/:key`
- Получить информацию о конкретном файле

**GET** `/api/s3/download/:key`
- Скачать файл

**GET** `/api/s3/preview/:key`
- Получить подписанный URL для превью (действителен 1 час)

### Удаление файлов

**DELETE** `/api/s3/files/:key`
- Удалить один файл

**DELETE** `/api/s3/files`
- Удалить несколько файлов
- Body: `{ "keys": ["key1", "key2", ...] }`

### Health Check

**GET** `/api/health`
- Проверка статуса API

## 📁 Структура проекта

```
test_govniche/
├── server/
│   ├── index.js          # Главный файл сервера
│   ├── config/
│   │   └── s3.js        # Конфигурация AWS S3
│   └── routes/
│       └── s3.js        # API маршруты для S3
├── client/
│   ├── public/
│   │   └── index.html
│   └── src/
│       ├── App.js       # Главный компонент
│       ├── components/  # React компоненты
│       │   ├── FileUpload.js
│       │   └── FileList.js
│       └── services/
│           └── api.js   # API клиент
├── package.json
├── .env.example
└── README.md
```

## 🎨 Особенности UI

- Современный градиентный дизайн
- Drag & Drop загрузка файлов
- Индикаторы прогресса загрузки
- Адаптивный дизайн для мобильных устройств
- Иконки для разных типов файлов
- Массовый выбор и удаление файлов

## 🔒 Безопасность

- Не коммитьте `.env` файл в git
- Используйте IAM роли с минимальными необходимыми правами
- В продакшене используйте переменные окружения сервера
- Настройте CORS правильно для вашего домена

## 🐛 Решение проблем

### Ошибка "Bucket does not exist"
- Убедитесь, что bucket существует в AWS Console
- Проверьте правильность имени bucket в `.env`
- Убедитесь, что у пользователя есть права `s3:CreateBucket`

### Ошибка "Access Denied"
- Проверьте AWS credentials в `.env`
- Убедитесь, что IAM пользователь имеет необходимые права
- Проверьте регион (должен совпадать с регионом bucket)

### CORS ошибки
- Настройте CORS политику для S3 bucket
- Проверьте `CLIENT_URL` в `.env`

## 📝 Лицензия

ISC

## 👨‍💻 Разработка

Для разработки используйте:
- `nodemon` для автоматической перезагрузки сервера
- `concurrently` для одновременного запуска сервера и клиента
- React DevTools для отладки фронтенда
