#!/bin/bash

echo "🚀 Установка Node.js и npm..."

# Проверка наличия Node.js
if ! command -v node &> /dev/null; then
    echo "📦 Установка Node.js через apt..."
    sudo apt update
    sudo apt install -y nodejs npm
    
    # Проверка версии
    if command -v node &> /dev/null; then
        echo "✅ Node.js установлен: $(node --version)"
        echo "✅ npm установлен: $(npm --version)"
    else
        echo "❌ Ошибка установки Node.js"
        echo "💡 Попробуйте установить вручную:"
        echo "   sudo apt install nodejs npm"
        exit 1
    fi
else
    echo "✅ Node.js уже установлен: $(node --version)"
    echo "✅ npm уже установлен: $(npm --version)"
fi

echo ""
echo "📦 Установка зависимостей проекта..."

# Установка бэкенд зависимостей
echo "📦 Установка бэкенд зависимостей..."
npm install

# Установка фронтенд зависимостей
echo "📦 Установка фронтенд зависимостей..."
cd client && npm install && cd ..

echo ""
echo "✅ Установка завершена!"
echo ""
echo "📝 Следующие шаги:"
echo "1. Отредактируйте файл .env и укажите ваши AWS credentials"
echo "2. Запустите приложение: npm run dev"
echo ""
