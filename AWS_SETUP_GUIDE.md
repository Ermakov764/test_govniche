# 📚 Подробная инструкция по настройке AWS S3 для чайников

## Что такое AWS и зачем это нужно?

AWS (Amazon Web Services) - это облачный сервис, где можно хранить файлы. S3 - это как "облачный диск" от Amazon, где ваше приложение будет хранить файлы.

Чтобы ваше приложение могло работать с AWS, нужно:
1. Создать аккаунт в AWS (если его нет)
2. Создать пользователя с правами доступа
3. Получить ключи доступа (credentials)
4. Создать "ведро" (bucket) для файлов
5. Указать эти данные в файле .env

---

## ШАГ 1: Создание аккаунта AWS (если его нет)

1. Откройте браузер и перейдите на https://aws.amazon.com/
2. Нажмите кнопку **"Создать бесплатный аккаунт"** (Create a Free Account)
3. Заполните форму регистрации:
   - Email адрес
   - Пароль
   - Имя
   - Номер телефона
   - Платежная карта (для верификации, но первые 12 месяцев есть бесплатный уровень)
4. Подтвердите email и телефон
5. Войдите в консоль AWS: https://console.aws.amazon.com/

**Важно:** У AWS есть бесплатный уровень (Free Tier), который позволяет хранить до 5GB файлов бесплатно в течение 12 месяцев.

---

## ШАГ 2: Создание IAM пользователя (для безопасности)

**Почему нужен отдельный пользователь?** 
Использовать корневой аккаунт небезопасно. Лучше создать отдельного пользователя с ограниченными правами.

### 2.1. Откройте IAM консоль

1. Войдите в AWS Console: https://console.aws.amazon.com/
2. В поиске вверху введите **"IAM"** и нажмите Enter
3. Откройте сервис IAM (Identity and Access Management)

### 2.2. Создайте нового пользователя

1. В левом меню нажмите **"Users"** (Пользователи)
2. Нажмите кнопку **"Create user"** (Создать пользователя)
3. Введите имя пользователя, например: **"s3-storage-user"**
4. Нажмите **"Next"** (Далее)

### 2.3. Настройте права доступа

1. Выберите **"Attach policies directly"** (Прикрепить политики напрямую)
2. В поиске введите **"S3"**
3. Найдите и отметьте галочкой: **"AmazonS3FullAccess"**
   - Это даст полный доступ к S3 (для простоты)
   - В продакшене лучше использовать более ограниченные права
4. Нажмите **"Next"** (Далее)
5. Нажмите **"Create user"** (Создать пользователя)

### 2.4. Сохраните Access Keys (КЛЮЧЕВОЙ МОМЕНТ!)

1. После создания пользователя вы увидите страницу успеха
2. **ВАЖНО:** Нажмите **"Create access key"** (Создать ключ доступа)
3. Выберите вариант использования: **"Application running outside AWS"** (Приложение вне AWS)
4. Поставьте галочку на согласие
5. Нажмите **"Next"** (Далее)
6. Нажмите **"Create access key"** (Создать ключ доступа)

**ВНИМАНИЕ!** Вы увидите два важных значения:

- **Access key ID** - это как логин (например: `AKIAIOSFODNN7EXAMPLE`)
- **Secret access key** - это как пароль (например: `wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY`)

**⚠️ КРИТИЧЕСКИ ВАЖНО:**
- Скопируйте оба значения СРАЗУ и сохраните в безопасном месте
- **Secret access key** показывается ТОЛЬКО ОДИН РАЗ!
- Если вы закроете это окно, Secret key будет потерян навсегда
- Придется создавать новый ключ

7. Нажмите **"Done"** (Готово)

---

## ШАГ 3: Создание S3 Bucket (ведра для файлов)

**Что такое Bucket?** Это как папка в облаке, где будут храниться ваши файлы.

### 3.1. Откройте S3 консоль

1. В поиске AWS Console введите **"S3"**
2. Откройте сервис **S3**

### 3.2. Создайте Bucket

1. Нажмите кнопку **"Create bucket"** (Создать bucket)
2. **Bucket name** (Имя bucket):
   - Введите уникальное имя (например: `my-test-storage-2026`)
   - Имя должно быть уникальным во всем мире AWS
   - Могут быть только маленькие буквы, цифры, дефисы и точки
   - Если имя занято, попробуйте другое (добавьте дату или случайные цифры)
3. **AWS Region** (Регион):
   - Выберите ближайший регион (например: `us-east-1` для США восток, `eu-west-1` для Европы)
   - Запомните выбранный регион - он понадобится для .env файла
4. **Object Ownership** (Владение объектами):
   - Оставьте **"ACLs disabled"** (рекомендуется)
5. **Block Public Access** (Блокировка публичного доступа):
   - Снимите галочку **"Block all public access"** (если хотите публичный доступ к файлам)
   - Или оставьте (если файлы только для вас)
6. Прокрутите вниз и нажмите **"Create bucket"** (Создать bucket)

### 3.3. Запомните данные

Запишите:
- **Имя bucket** (например: `my-test-storage-2026`)
- **Регион** (например: `us-east-1`)

---

## ШАГ 4: Настройка файла .env

Теперь нужно указать все полученные данные в файле `.env` вашего проекта.

### 4.1. Откройте файл .env

```bash
cd /home/user/Документы/test_govniche
nano .env
```

Или откройте файл в любом текстовом редакторе.

### 4.2. Замените значения

Найдите в файле эти строки:

```env
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_REGION=us-east-1
AWS_S3_BUCKET_NAME=your-bucket-name
```

И замените на ваши реальные значения:

**Пример:**

```env
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
AWS_REGION=us-east-1
AWS_S3_BUCKET_NAME=my-test-storage-2026
```

**Где что брать:**

1. **AWS_ACCESS_KEY_ID** - это тот самый **Access key ID**, который вы скопировали в шаге 2.4
2. **AWS_SECRET_ACCESS_KEY** - это тот самый **Secret access key**, который вы скопировали в шаге 2.4
3. **AWS_REGION** - это регион, который вы выбрали при создании bucket (например: `us-east-1`, `eu-west-1`)
4. **AWS_S3_BUCKET_NAME** - это имя bucket, которое вы создали (например: `my-test-storage-2026`)

### 4.3. Сохраните файл

- В nano: нажмите `Ctrl+O` (сохранить), затем `Enter`, затем `Ctrl+X` (выйти)
- В других редакторах: просто сохраните файл

---

## ШАГ 5: Проверка работы

1. Убедитесь, что сервер перезапустился (он должен автоматически перезапуститься после изменения .env)
2. Откройте браузер: http://localhost:3000
3. Попробуйте загрузить тестовый файл
4. Если все работает - вы увидите файл в списке!

---

## Частые проблемы и решения

### Ошибка: "InvalidAccessKeyId"
- **Причина:** Неправильный Access Key ID
- **Решение:** Проверьте, что вы скопировали Access Key ID правильно, без лишних пробелов

### Ошибка: "SignatureDoesNotMatch"
- **Причина:** Неправильный Secret Access Key
- **Решение:** Проверьте Secret Access Key. Если потеряли - создайте новый в IAM

### Ошибка: "NoSuchBucket"
- **Причина:** Неправильное имя bucket или регион
- **Решение:** Проверьте имя bucket и регион в .env файле

### Ошибка: "Access Denied"
- **Причина:** У пользователя нет прав на bucket
- **Решение:** Убедитесь, что пользователь имеет политику `AmazonS3FullAccess`

---

## Безопасность

⚠️ **ВАЖНЫЕ ПРАВИЛА:**

1. **НИКОГДА** не публикуйте файл `.env` в Git
2. **НИКОГДА** не делитесь своими AWS credentials с другими
3. Если credentials попали в чужие руки - **СРАЗУ** удалите их в AWS и создайте новые
4. Используйте разные credentials для разработки и продакшена

---

## Дополнительная помощь

Если что-то не работает:

1. Проверьте логи сервера в терминале
2. Убедитесь, что все значения в .env правильные
3. Проверьте, что bucket существует в AWS Console
4. Убедитесь, что у пользователя есть права на S3

---

## Краткая шпаргалка

```
1. AWS Console → IAM → Create User → Create Access Key → СОХРАНИТЬ КЛЮЧИ!
2. AWS Console → S3 → Create Bucket → ЗАПОМНИТЬ ИМЯ И РЕГИОН!
3. Открыть .env → Вставить ключи и данные bucket → Сохранить
4. Готово! 🎉
```
